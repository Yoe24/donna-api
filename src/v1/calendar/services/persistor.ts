// V1 Inbox to Calendar — Phase 4 — STUB Phase 1
// PERSIST: insert events_v1 with status (auto/to_verify/dismiss) per confidence thresholds.
// Implementation comes in Phase 4.

import type { ExtractedEvent } from '../schemas';

export async function persistEvent(_userId: string, _event: ExtractedEvent): Promise<string> {
  throw new Error('persistEvent not implemented yet (Phase 4)');
}

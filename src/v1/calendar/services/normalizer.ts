// V1 Inbox to Calendar — Phase 4 — STUB Phase 1
// NORMALIZE + DEDUPE: canonicalize date/time/tz, compute dedup hash.
// Implementation comes in Phase 4.

import type { ExtractedEvent } from '../schemas';

export function normalizeEvent(_event: ExtractedEvent): ExtractedEvent {
  throw new Error('normalizeEvent not implemented yet (Phase 4)');
}
